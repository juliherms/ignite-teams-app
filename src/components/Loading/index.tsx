import { Container, LoadingIndicator } from "./styles";

// o load indicator é um componente que representa um carregamento

export function Loading() {
    return(
        <Container>
            <LoadingIndicator />
        </Container>
    );
}